services:
  flask:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - db
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: spendsmart
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
